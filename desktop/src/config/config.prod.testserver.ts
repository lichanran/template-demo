import {Config} from './config-types'

export const config: Config = {
    hosts: {
        // 测试的window 服务器
        base: "http://10.24.22.175:8888",
    },
    successCode: 0,
    baseHref: "/modelmarket/",
    enableActionLogger: false,
}